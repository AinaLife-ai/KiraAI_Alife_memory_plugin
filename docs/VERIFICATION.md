# 验证记录

## 2.1.0 本轮验证（2026-09-08）

基于上游 main `c62f60476ab3dc61ac13ba007f58c55c449cc94c` 新建干净分支；重新核对 KiraAI HEAD 仍为 `cad046f7c6a4b071803debe589fb26881ff5fc73`。

**45项测试、7个参数化子场景通过**。Ruff、JS语法和前端格式检查通过。新增覆盖：

- 受控超时先失败，再验证重试成功；大批次6→3→2缩减后成功、全部原文与子链接保留；失败任务有可操作说明，自动冷却、手动重试；格式错误与网络故障共用总重试预算。
- 使用真实 `openai.APITimeoutError/APIConnectionError` 重现SDK提前失败，再验证KiraAI模型调用边界归一化后能重试，不只测试内置TimeoutError。
- 旧数据库升级前在线备份可读取，重复初始化不覆盖备份，旧记录逐字段一致；不修改原有记忆文件和层级。
- 新装global跨会话本地召回携带sid、users、时间和名称，无关内容排除；“猫”单字仍可检索；system前缀及原聊天历史稳定、向量默认不调用。
- 现名/曾用名查询，同名不同ID独立；迟到消息不回滚新昵称；过期编辑冲突；linked范围名称访问包含同参与者的历史存档；OneBot按真实实例名查询，未知ID不猜平台。
- 旧“认为”关系保留但隔离，模型新输出拒绝、审计可修正完整连线；纯删除允许清理旧坏关系，混合非法补丁仍拒绝；删除最后来源后相应事实停止召回。

浏览器：本地真实插件API、核心bridge脚本、**与KiraAI一致的iframe sandbox属性**、合成记忆。复现旧删除点击无效；新页内确认后记录20→19。实测新增、关键词搜索、移出/恢复永久上下文；昵称更名保留曾用名；关系图/维度图切换与视觉检查；配置说明含主动会话示例；CSS热更新恢复未保存180秒配置并保存成功。另复现嵌入页未启动导出下载，改为页内完整JSON与复制，验证显示v3记录/名称历史及“已复制完整内容”。独立页面保留下载入口。

两路独立静态审查：规范审查2项、需求审查2项P2均已修正（删除校验、linked名称范围、SDK异常与中文单字），另修正文档默认范围。前端拆分后统一格式化便于维护。

边界：没有用户生产模型延迟样本，不能断言服务商超时的具体成因或真实生成质量。OneBot测试使用符合当前核心接口的响应夹具；未连接真实QQ、未修改用户实际数据库。UI测试模拟实际沙箱与bridge，没有启动整套KiraAI管理端。父摘要在删除子记录后仍保留，界面已明确说明；模型语义正确性仍需来源核对。

## 2.0.0 历史验证

环境：Windows、Python 3.12、Pydantic 2.13；KiraAI v2.34.1，固定核心提交见 PORTING.md。

最终本地结果：**32 项测试通过，7 个参数化子场景通过**；Ruff 静态检查、JavaScript 语法检查和 `git diff --check` 通过。

已执行：

- 数据与引擎回归：原文保留、父子溯源、级联、逆序区域、同秒唯一ID、重复事件、编辑竞态事务回滚、永久记忆移出后读取。
- 检索与分类：Unicode关键词、时间交集、分页总数、会话范围、向量模型隔离、未知来源拒绝、同实体合并保留证据、跨实体合并拒绝。
- 结构化输出：围栏、前后解释、重复键、额外字段、缺字段、类型漂移、NaN均拒绝；有界重试；配置错误拒绝。
- 后台：任务去重、单次认领、重启恢复、取消重排队、配置变化放弃过期提交。
- 停用时等待 SQLite 工作线程真正完成，避免协程已取消但数据库句柄仍占用的竞态；另有确定性阻塞线程测试。
- 真实KiraAI类集成：插件导入和注册、KiraMessageBatchEvent捕获、LLMRequest上下文与非持久注入、原生工具、管理员API配置原子保存、记忆409冲突及422校验。
- 安全迁移：120/121 字符边界、默认插件长文本保留、多行/转义/时间戳、来源文件逐字节哈希不变、两次导入去重、多来源凭据、人工删除不复活、阈值调整重试、事务注入故障回滚、共享范围压缩后不丢失。
- 互斥：先提交后停用、解析错误保持旧插件、收尾快照失败恢复旧插件、手动切回旧插件后 Alife 让位。使用真实核心类与受控插件管理器替身，不声称完成生产插件生命周期验收。
- 缓存：修改永久记忆并增加感知后，两次真实 LLMRequest 的 system 内容及已有聊天消息完全一致，动态内容只出现于末尾 user 区域且先于本轮输入。
- 默认无向量：客户端解析入口设置为调用即报错，搜索/索引/遗留重建任务均通过；关闭时管理员重建接口拒绝排队。本地词语检索过滤无关项。
- 可选向量：显式开启后能生成索引；请求进行中关闭后丢弃返回向量，不写入过期索引。
- JavaScript语法检查。
- 浏览器：本地真实插件API + 合成演示数据，配置保存立即生效；记忆详情、编辑器与原文入口；修改前端文件后自动重载，未保存编辑草稿恢复，并成功保存到存档列表。
- 本次浏览器补测：默认/KiraOS 迁移共 3 条合成记忆、超长跳过 1 条的报告；重新迁移按钮可用；默认向量开关关闭且模型选择禁用，勾选后可选，关闭保存后向量重建按钮禁用。

测试模型为确定性夹具，不调用用户的生产模型或聊天适配器。**未执行**：真实模型质量/成本评估、真实QQ/Telegram等端到端主动发送、目标用户已有记忆库迁移、与原版同硬件性能对比。界面是在本地验证宿主中运行；KiraAI整套管理端的实际嵌入部署仍需安装验收。

建议安装验收顺序：配置压缩/审计模型 → 小批量对话 → 核对原文和归类 → 读取子存档 → 编辑与重载 → 核对范围隔离 → 开启指定测试会话的主动感知。
